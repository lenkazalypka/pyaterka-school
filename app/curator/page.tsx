import {RoleHome} from "@/components/role-home";import{requireRole}from"@/lib/auth";export const dynamic="force-dynamic";export default async function Page(){await requireRole("curator");return <RoleHome title="Кабинет куратора" description="Риски и отчёты только по закреплённым ученикам." items={["Закреплённые ученики","Просрочки и риски","Отчёты и рекомендации"]}/>}
